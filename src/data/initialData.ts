import { Subject, TimetableSlot, Task, SemesterResult, StudyGoal, CalendarEvent, Reminder, StudySession } from '../types';

export const initialSubjects: Subject[] = [];

export const initialTimetable: TimetableSlot[] = [];

export const initialTasks: Task[] = [];

export const initialSemesters: SemesterResult[] = [];

export const initialGoals: StudyGoal[] = [];

export const initialCalendarEvents: CalendarEvent[] = [];

export const initialReminders: Reminder[] = [];

export const initialStudySessions: StudySession[] = [];

